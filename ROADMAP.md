# ROADMAP — ZHELEZO (zhelezo.space)

*Создан: 2026-07-21, по запросу пользователя ("автономный Growth Marketing Engineer и SEO-специалист" бриф). Консолидирует всё сделанное по росту/SEO с 2026-07-18, что сделано в этом проходе, что осознанно отклонено (с обоснованием) и что дальше.*

Полная история по сессиям — `Память/index.md` и `Память/sessions/*.md`. Этот файл — рабочая витрина для роста, а не замена журналу сессий.

## 1. Технический фундамент — сделано

- Полный SEO/CRO/perf/a11y/security аудит лендинга, HSTS, CVE-фиксы, code-splitting, non-blocking fonts, custom 404, ErrorBoundary + client-side error reporting.
- CSP на основе точных sha256-хэшей инлайн-скриптов, CI-guard (`scripts/verify-csp-hashes.mjs`) — забытый хэш валит билд, а не тихо ломает скрипт в проде.
- **Полная мультиязычность RU/EN/VI**: 3 статических entry point'а (`/`, `/en/`, `/vi/`), собственные словари (не машинный перевод "как есть"), hreflang на всех 30 страницах, sitemap.xml на 27 URL с alternates, языковой переключатель + автоопределение браузера + localStorage, subId-разметка на каждой ссылке. Верифицировано Playwright (не только `tsc`/build), не гостовый — реально кликано/проверено.
- Блог: 7 статей × 3 языка = 21 страница, BreadcrumbList+Article JSON-LD, полная перелинковка.
- Privacy/Terms переведены на все 3 языка (переведены по смыслу, не построчно).
- **Известный операционный риск, зафиксированный и не повторявшийся с 2026-07-20**: у Cloudflare Pages для этого проекта нет GitHub auto-deploy — деплой только вручную (`npm run deploy` из `Landing/`), с обязательной `curl`-проверкой живого домена после. `git push` сайт не деплоит.

## 2. Сделано в этом проходе (2026-07-21)

### Реальный технический аудит (не гадание — Lighthouse + прод)
- Прогнан настоящий Lighthouse (mobile, simulated throttling) против `https://www.zhelezo.space/`. **До**: Performance 42, LCP 6.1с, TBT 1480мс, Speed Index 5.5с. Accessibility/SEO уже были 100.
- **Найдена и исправлена реальная причина**: GTM (148 КБ) и Яндекс.Метрика (94 КБ + 13 КБ webvisor-скрипт) уже грузились `async`, но их *выполнение* конкурировало с главным потоком браузера прямо в окне отрисовки LCP-элемента (H1). Решение — official vendor queuing pattern: `gtag()`/`ym()`-стабы определяются сразу (это чистый push в массив, без сети), а фактическая загрузка `gtag.js`/`tag.js` отложена до `window.load` + `requestIdleCallback` (фоллбэк `setTimeout`). Ни одно событие не теряется — оба стаба гарантированно ставят вызовы в очередь и реплеят их после реальной загрузки скрипта; это стандартный паттерн, а не хак.
- Задеплоено на все 3 языковые версии (RU/EN/VI, идентичный код), CSP-хэши пересчитаны и провалидированы (`verify-csp-hashes.mjs`), curl-проверка на проде подтвердила выкладку.
- **После**: Performance **42 → 82**, LCP **6.1с → 3.5с**, TBT **1480мс → 80мс**, Speed Index 5.5с → 4.8с. Измерено повторным прогоном Lighthouse на живом сайте, не предположение.
- robots.txt — корректен (`Allow: /`, ссылка на sitemap). sitemap.xml — 200 OK. Иконки/манифест — все резолвятся (`icon-192.png`, `icon-512.png`, `site.webmanifest`, `apple-touch-icon.png`); `/favicon.ico` отдаёт 404, но это не проблема — в `<head>` объявлен `<link rel="icon" href="/favicon.svg">`, который современные браузеры используют вместо неявного запроса `/favicon.ico`. Не чиню — не стоит усилий.
- Best Practices застрял на 77 — единственная причина: сторонние cookie от Яндекс.Метрики (`bh`, `yabs-sid`), которые Chrome помечает как "third-party cookies" в рамках депрекации 3p-cookies. Это часть работы самого счётчика Метрики (session recording/webvisor), не баг кода сайта — если понадобится поднять этот балл, вариант один: выключить `webvisor:true` в инициализации Метрики, но это осознанный компромисс аналитики vs. Lighthouse-балла, не техдолг.

### Индексация — статус на 2026-07-21 (обновлено)
- Ранее (в начале дня): дважды проверено через живой веб-поиск — домен не появлялся в выдаче вообще, Google Search Console не была подключена (заготовка `<meta name="google-site-verification">` в `index.html` осталась закомментированным плейсхолдером, никогда не заполнялась реальным кодом).
- **Обновление от пользователя (2026-07-21, позже тем же днём): Google Search Console подтверждена, sitemap.xml отправлен и доступен по `https://www.zhelezo.space/sitemap.xml`.** Верификация прошла НЕ через HTML-тег (он так и остался плейсхолдером в комментарии — не трогать, это не баг, просто верификация состоялась другим методом, вероятно через уже установленную GA4-привязку). Пользователь явно сказал считать задачи по подтверждению сайта и sitemap выполненными.
- Bing (`msvalidate.01`) и Яндекс.Вебмастер (`yandex-verification`) verification-теги на сайте есть; не проверено (и не блокер), была ли sitemap явно отправлена и там же.
- **Осталось, необязательно/по желанию**: в панели GSC — URL Inspection → Request Indexing вручную для главных страниц (`/`, `/en/`, `/vi/`, блог-индекс), это ускоряет первый краул на дни-недели вместо ожидания органического обнаружения. Чисто ручное действие в веб-интерфейсе, не блокер — Google и так найдёт sitemap сам через некоторое время.

## 3. Каналы продвижения — статус (честно, по реальным правилам площадок)

### Уже сделано / контент готов
| Канал | Статус |
|---|---|
| Блог (SEO) | 21 статья (7 × 3 языка), полная перелинковка, hreflang |
| X (Twitter) | 14-дневный календарь (`Texts/x_calendar_14day.md`), органика без фабрикации |
| Reddit | 2 поста без ссылок в теле (по актуальным анти-аффилиатским правилам сабреддитов), `Texts/reddit_content.md` |
| Quora | 10 шаблонов ответов под реальные вопросы (`Texts/quora_answers.md`) — самопостинг вопрос+ответ от себя отклонён как манипулятивный паттерн |
| bitcointalk | Календарь набора истории + 7 подписей (`Texts/forum_bitcointalk.md`) |
| Discord | Структура сервера + бот-код готовы (`Texts/discord_server_structure.md`, `Scripts/discord-bot/`) — не задеплоено, нет токена/хостинга (пользователь) |
| Guest-post питчи | Список площадок готов (`Texts/casino_directories.md`) — рассылка вручную (не автоматизирую холодные питчи третьим лицам без явного контроля) |

### Явно отклонено — с обоснованием (не "риск", а прямой запрет площадки)
| Канал | Причина отказа |
|---|---|
| **Pinterest** | Community Guidelines прямо запрещают "online gambling and lotteries" |
| **Medium** | Официальные правила прямо запрещают "facilitation of gambling or betting" |
| **VK** | Правила (п.3.9) запрещают рекламу казино без исключений даже для органики; группа будет заблокирована |
| **LinkedIn** | Аудитория B2B/карьера не пересекается с ЦА казино-афилиата ни по одному параметру |
| **Product Hunt** | Площадка для запуска софт-продуктов; гемблинг-контент удаляется/проваливается по гайдлайнам |
| **Hacker News** | Мгновенный бан за самопромо; аудитория разработчиков нетерпима к аффилиатскому контенту |
| **Dev.to / Hashnode** | Та же логика, что HN — площадки для тех-контента, аудитория не пересекается |
| **AlternativeTo / BetaList** | Каталоги софт-продуктов/стартапов — ZHELEZO категориально не туда |
| **GitHub README/Pages как канал промо** | Тот же аргумент, что Dev.to/HN — аудитория GitHub (разработчики) не ЦА казино-афилиата, репозиторий не открытый проект-приманка. Не создаю ради "промо" |

### Низкий приоритет / условно разрешено, контента пока нет
| Канал | Позиция |
|---|---|
| **Facebook/Instagram** | Meta требует gambling ads pre-approval + лицензию оператора для платной рекламы (нет у обычного аффилиата) — платное продвижение исключено. Органика возможна только с акцентом на "крипто/Web3", без слов "казино"/"ставки" в лоб — контент-пакета пока нет, не приоритет против уже работающих каналов |
| **Threads** | Те же ограничения Meta — только образовательный контент, ссылка только в bio, не приоритет |
| **Bluesky** | Не запрещено, но маленькая база относительно X — кросспост X-контента без доп. усилий, если появится аккаунт; не отдельная кампания |

## 4. Аналитика — статус
- GA4 и Яндекс.Метрика — установлены и работают (после фикса из раздела 2 — с отложенной загрузкой).
- Bing Webmaster Tools и Яндекс.Вебмастер — verification-теги на месте.
- Google Search Console — **подтверждена пользователем 2026-07-21, sitemap отправлен** (см. раздел 2). Больше не блокер.
- Meta Pixel / TikTok Pixel / Microsoft Clarity — код-заготовки в `index.html` закомментированы, ждут реальных ID от пользователя.

## 5. Мониторинг — сделано в этом проходе
- Новый `Scripts/brand-monitor/site-health-check.mjs`: реальные HTTP-проверки robots.txt/sitemap.xml/каждого URL из sitemap + доп. внутренних ссылок, статус-коды + время отклика, порог "медленно" >2с. Без зависимостей (как и `check-mentions.mjs`), честно документирует свой предел — не видит ссылки внутри React SPA (`/`, `/en/`, `/vi/` рендерятся клиентом, в сыром HTML нет `<a href>`).
- **Реальная находка от первого же запуска**: `privacy-policy`/`terms` (×3 языка, 6 страниц) существовали и работали (200 OK), но отсутствовали в `sitemap.xml` и были видны только из клиентского рендера Footer — невидимы для любого краулера без JS. Исправлено: 6 URL добавлены в `sitemap.xml` с hreflang-alternates по той же схеме, что у остальных страниц; задеплоено, подтверждено curl (sitemap теперь 33 URL вместо 27), повторный прогон скрипта — 0 проблем.

## 6. Дальше (следующие шаги)
- ~~После получения GSC-кода от пользователя...~~ **Готово** — GSC подтверждена, sitemap отправлен (см. раздел 2). Осталось необязательное: Request Indexing вручную для главных URL в панели GSC, если хочется ускорить первый краул.
- Отложенная (не забытая) тема статьи блога — честный разбор жалоб на заморозку выигрышей у BC.Game, требует аккуратной фактологической подачи (см. `REPORT/GROWTH_PLAN.md`, раздел "Keyword-исследование — 2026-07-21").
- Периодический запуск `site-health-check.mjs`/`check-mentions.mjs`: обёртка `Scripts/brand-monitor/run-checks.bat` написана и вручную протестирована end-to-end (2026-07-21, чистый прогон с нуля — 33/33 URL здоровы, 0 упоминаний бренда пока не проиндексировано). **Обновление**: регистрация в Task Scheduler выполнена (см. раздел 8) — пользователь дал явное разрешение.
- Партнёрская панель BC.Game: LTV/retention по subId проверены (60 дней, сотни визитов, 0 депозитов) — открытый вопрос "почему конверсия нулевая при живом трафике" остаётся нерешённым, требует данных из панели BC.Game, к которым у меня нет доступа.

## 7. Сделано в этом проходе (2026-07-21, второй заход — роль CTO/Growth Engineer передана основному потоку)

- **Реальный а11y-баг**: не было skip-to-content ссылки — клавиатурный/скринридер-пользователь был вынужден таб-ать через весь sticky-header на каждой странице, прежде чем добраться до контента. Добавлена стандартная sr-only/focus-visible ссылка (переведена RU/EN/VI) на новый `id="main-content"` с `tabIndex={-1}` на `<main>`.
- **Реальный а11y-баг**: у выпадающего меню LanguageSwitcher не было закрытия по Escape — клавиатурный пользователь не мог закрыть меню иначе как кликом мимо. Добавлен обработчик Escape с возвратом фокуса на кнопку-триггер. При первой реализации сам поймал баг с "протухшим" замыканием (пустой массив зависимостей эффекта означал, что `open` внутри обработчика всегда было `false`) — исправлено добавлением `open` в зависимости эффекта, до деплоя.
- Проверено и оставлено как есть: `npm audit` чист в Landing и SalesAgent (0 уязвимостей), продакшн-бандл Landing уже минимален (4 прод-зависимости, все нужны продукту — framer-motion не стал выпиливать ради небольшой экономии КБ, риск/выгода не в пользу рефакторинга), redirect-цепочки без лишних хопов, SalesAgent (`mailer.mjs`, `bin/send.mjs`) синтаксически и логически чист при повторном ревью.
- Написана и вручную протестирована (не просто написана — реально прогнана с нуля) обёртка автоматизации мониторинга (см. раздел 6) — сама постановка в Task Scheduler оставлена пользователю на явное разрешение.
- Все изменения задеплоены на прод и подтверждены `curl`'ом (первая проверка поймала краевой кэш CDN сразу после деплоя — не приняла её на веру, перепроверила через несколько секунд).

## 8. Сделано в этом проходе (2026-07-21, третий заход)

- **Automation**: пользователь дал явное разрешение — `ZHELEZO-BrandMonitor` зарегистрирован в
  Windows Task Scheduler (ежедневно 09:00, `schtasks /query` подтверждает статус Ready).
- **Реальный а11y-пробел, закрыт полностью**: ни одна из 17 внешних ссылок сайта (`target="_blank"`)
  не объявляла скринридеру, что открывается новое окно/вкладка — стандартный WCAG best-practice
  паттерн, которого не было. Добавлен переведённый (RU/EN/VI) visually-hidden span
  `t.opensInNewWindow` в конец каждой ссылки; декоративные иконки внутри (`ArrowRight`, `Coins`,
  `ExternalLink`, `PlayCircle`) получили `aria-hidden="true"`, чтобы не задваивать объявление.
  Соотношение 17 ссылок / 17 меток подтверждено `grep` до коммита. Задеплоено, подтверждено
  `curl`'ом напрямую по JS-бандлу (`LocaleContext-*.js`) — по одному вхождению строки на каждый
  из 3 языков.
- Билд/typecheck (`tsc && vite build`) и CSP-hash guard (`verify-csp-hashes.mjs`) прошли чисто
  перед деплоем.

## 9. Сделано в этом проходе (2026-07-21, четвёртый заход — свежий скептический аудит)

Цель прохода: не считать, что заходы 1-3 закрыли всё, перепроверить с нуля области, которые
ещё не аудировались явно (изображения, шрифты, состав бандла, canonical-теги, og:image,
формы, 404-страница, тех.долг SalesAgent). Результат — один реальный, стоящий фикса баг;
остальное честно закрыто как "проверено, найдено чистым" без искусственной работы ради счёта.

- **Реальный баг, исправлен**: `public/404.html` был русскоязычным для абсолютно любого
  битого пути, включая `/en/...` и `/vi/...` — Cloudflare Pages отдаёт один статический
  404-файл на все несовпавшие пути независимо от языковой секции, отдельного 404 на
  директорию в статическом хостинге не бывает. Добавлен небольшой inline-скрипт без
  зависимостей: читает языковой префикс из `location.pathname`, подменяет
  заголовок/`<h1>`/текст/ссылку "на главную" на EN/VI; RU (без префикса) не тронут, работает
  как раньше. Задеплоено, HTTP-код 404 сохранён (подтверждено `curl`), наличие
  скрипта-локализатора подтверждено в ответах `/en/` и `/vi/`.
- **Проверено, оставлено как есть (реальный аудит, не beглый взгляд)**:
  - Изображения — все статические ассеты ≤56 КБ (og-image.png, иконки), оптимизировать
    нечего, сайт не несёт тяжёлого фотоконтента.
  - Шрифты — уже `preconnect`+`preload`+`display=swap`+`<noscript>`-фоллбэк, best practice.
  - Состав JS-бандла — крупнейший чанк (`bootstrap`) 266 КБ / 86 КБ gzip, разумно для
    React-приложения такого размера, признаков раздутости нет.
  - Canonical-теги — все 24 HTML-файла блога (RU/EN/VI × 8 статей+индексов) плюс 3 версии
    главной корректно self-referencing, проверено построчно, не выборочно.
  - `og:image:alt` — одинаковый на всех 3 языках ("ZHELEZO Crypto Gaming") сознательно: это
    имя бренда, как и `<title>`/OG-заголовки уже показывают тот же паттерн, не забытый перевод.
  - Формы на сайте отсутствуют — валидировать нечего.
  - SalesAgent — свежий `npm audit` чист (0 уязвимостей), `node --check` по всем `.mjs`
    файлам чист, TODO/FIXME/XXX не найдено — тех.долг не накопился несмотря на рост
    (SMTP-отправка, 39+ лидов, несколько раундов анализатора).
- Билд/typecheck и CSP-hash guard прошли чисто перед деплоем.

## 10. Сделано в этом проходе (2026-07-21, пятый заход — безопасность, SQL-слой SalesAgent, CRO, тех.долг)

Фокус: заходы 3-4 закрыли изображения/шрифты/бандл/canonical/og:image/формы/поверхностный
SalesAgent-аудит. Этот заход — то, на что не хватило бюджета: заголовки безопасности +
секреты в git-истории, SQL-слой недавно выросшего кода SalesAgent, CRO дальше Hero.tsx,
мёртвый код Landing.

- **Реальный баг, исправлен**: `SalesAgent/src/reports/index.mjs`'s `generateStats({ since })`
  строил все `WHERE`-условия сырой интерполяцией строки (`WHERE created_at >= '${since}'`)
  вместо параметризованных `?`, которые используются во всём остальном коде (`crm/index.mjs`).
  Не эксплуатируется через два существующих вызова сегодня (`bin/report.mjs` передаёт только
  фиксированную внутреннюю дату из белого списка периодов; `dashboard/generate.mjs` вызывает
  без аргументов) — но это экспортируемая, переиспользуемая функция, принимающая произвольную
  строку `since`; любой будущий вызов с пользовательским вводом стал бы реальной инъекцией.
  Переписано на параметризованные запросы. Проверено: существующие вызовы (`report.mjs`,
  `dashboard.mjs`) работают как прежде, реальный фильтр по дате действительно фильтрует,
  инъекция `OR '1'='1'` больше не обходит фильтр (0 совпадений вместо полного count).
  Коммит `586e2d0`.
- **Проверено, оставлено как есть**:
  - Заголовки безопасности (`Landing/public/_headers`) — CSP на точных sha256-хэшах (без
    `unsafe-inline` в script-src), HSTS с preload, X-Frame-Options DENY, Referrer-Policy,
    X-Content-Type-Options, Permissions-Policy — добавлять нечего.
  - Секреты в git-истории — полный `git log --all -p` по паттернам api-key/secret/password/
    token, единственное совпадение — плейсхолдер `your-16-char-app-password` в
    `.env.example`. Файл `.env` никогда не был закоммичен ни разу.
  - CRO дальше Hero.tsx — `UrgencyTimer` уже честный (повторяющееся часовое окно, не разовый
    фейковый дедлайн, задокументировано юридическое обоснование в комментарии). CTA-точек по
    странице 9+ (hero/bonus/games×N/how-it-works/footer×7/header/lucky-wheel), все с
    трекингом. `Header` — `fixed top-0`, значит мобильный доступ к CTA уже решён без
    отдельного sticky-бара — не стал придумывать фичу под уже закрытую проблему.
  - Мёртвый код Landing — `tsconfig.json` уже держит `noUnusedLocals`/`noUnusedParameters`
    включёнными на каждом билде (значит JS-уровень мёртвого кода ловится автоматически, не
    накапливается), Tailwind JIT не генерирует неиспользуемые классы по построению — оба
    классических источника техдолга здесь структурно предотвращены, а не просто "не найдено
    сейчас".
- Билд/typecheck прошли чисто (SalesAgent — синтаксис + функциональные smoke-тесты; Landing
  не менялся в этом заходе, кроме уже задеплоенного в предыдущих заходах).

## 11. Полный визуальный редизайн (2026-07-21, по явному брифу пользователя)

Не косметика — пользователь явно просил не ограничиваться мелкими правками. Новая палитра
(`#050505` чёрный / `#00FF66` неоновый зелёный / тёмно-зелёные карточки), canvas-анимированный
фон (пепел/искры/glow, с уважением к prefers-reduced-motion и Page Visibility API), glassmorphism
на всех карточках, scroll-reveal анимации, динамическая прозрачность Header при скролле, новые
`SectionDivider`/`BackToTop`/`FollowMe` (8 соцсетей-заглушек, ждут ссылок от пользователя),
pure-CSS preloader без цены в JS-бандле. Полные детали изменений — `CHANGELOG.md`, раздел
2026-07-21.

**Критическая проверка после деплоя**: свежий Lighthouse на проде показал **Performance
не регрессировал** (82, LCP даже улучшился 3.5с→3.2с), Accessibility/SEO остались 100/100,
CLS 0 — несмотря на добавленный canvas-фон и расширенное использование framer-motion. Это был
главный риск задачи (пользователь прямо просил не жертвовать Lighthouse-баллом ради анимаций),
и он не реализовался. Заголовки безопасности, все 33 URL sitemap, canonical/hreflang/schema —
без изменений.

**Дальше по редизайну**: пользователь сказал прислать реальные ссылки соцсетей для `FollowMe`
позже — заглушки готовы принять их в `src/components/FollowMe.tsx` (константа `SOCIALS`, поле
`href`). Больше ничего не отложено намеренно — вся заявленная функциональность брифа реализована
за этот проход.

## 12. Premium-эскалация редизайна (2026-07-21, тот же день — более амбициозный повторный бриф)

Пользователь поднял планку: ориентир на уровень NVIDIA/Razer/Alienware/Riot/Steam/Apple/Stripe,
"вау-эффект за 3 секунды". Добавлено поверх раздела 11 (не переписано с нуля): magnetic-hover
на 3 главных CTA, viewport-wide mouse-tilt, `EnergyCore` — CSS-собранный "3D"-объект в Hero
(кольца/пульсирующее ядро/орбитальные частицы, не WebGL — сознательный выбор ради Lighthouse),
`AnimatedCounter` на статистике Hero, shine-sweep и вращающаяся gradient-border (только на
самом заметном CTA — бонус-секции, не на всех карточках подряд), film-grain noise-оверлей.
Полные детали — `CHANGELOG.md`.

**Главный риск прохода — не реализовался**: добавлено несколько постоянно анимирующихся
элементов (вращающиеся кольца, орбитальные частицы, mouse-tilt/magnet-слушатели, noise) поверх
уже работающего canvas-фона. Свежий Lighthouse на проде после деплоя — Performance всё ещё
**82**, LCP 3.3с, CLS 0, Accessibility/SEO 100/100. Заголовки безопасности и все 33 URL sitemap
не пострадали.

**Честно про предел проверки**: mobile-адаптивность проверена по коду (брейкпоинты, `overflow-
hidden`, размеры орбит частиц относительно контейнера), не реальными скриншотами устройств —
в этой среде нет инструмента для визуального рендеринга/скриншотов. Если после ручной проверки
на реальном телефоне/планшете найдутся визуальные баги — это единственная область, которую я не
мог проверить сам.

## 13. AAA-gaming визуальный язык + перенос соцсетей/нового CTA в навигацию (2026-07-21, четвёртый заход)

Пользователь прямо попросил НЕ косметику поверх старого дизайна, а новый визуальный язык:
"полированный металл + стекло + неон + объём", ориентир — NVIDIA/Razer/Alienware/Riot/Steam.
Добавлено поверх разделов 11-12: chamfered HUD-рамки (`.chamfered`), светящиеся угловые скобки
(`CornerBrackets.tsx`), металлический текст заголовка (`.metal-text`), объёмные "клавиши"
(`.btn-metal`), новая панель по умолчанию `.hud-panel`. Полные детали — `CHANGELOG.md`.

**Реальная просадка Lighthouse на первом деплое (82 → 78, LCP 3.3с → 3.7с) — найдена и
исправлена, не отчитана как норма.** Причина: анимированный `background-position` +
двойной `drop-shadow` на `.metal-text`, применённый к H1 (LCP-элементу Hero). Убрана
анимация/лишний drop-shadow, статичный градиент — передеплой, Performance снова **82**, LCP
снова **3.3с**. Урок для будущих проходов закреплён комментарием в `index.css`: не класть
анимированные/фильтрованные эффекты на вероятные LCP-кандидаты (обычно это H1 hero-секции).

**Отдельно в этом же проходе — 2 конкретных запроса пользователя, не связанных с визуальным
языком**: соцсети вынесены из подвала в свою секцию (`Social.tsx`, `id="social"`); добавлен пункт
навигации "Начать играть" — прямая ссылка на реферальную ссылку, не скролл. Оба на RU/EN/VI.
