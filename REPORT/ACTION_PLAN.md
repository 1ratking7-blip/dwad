# Action Plan — путь к первому доходу

Дата: 2026-07-18. Консолидирует `TODO.md` + `GROWTH_PLAN.md` в один приоритизированный план.
Разбито на то, что сделано сейчас, что делать дальше без доп. доступа, и что блокируется на вас.

## Сделано в этой сессии

1. **Убран юридический риск в `SocialProof.tsx`** — фабрикованные "Live Win" были подписаны
   как реальные live-данные (прямой обман по GEMINI.md п.8). Переименовано в "Пример
   выигрыша", билд проверен (`npm run build` проходит).
2. **Контент-пакеты под каналы, одобренные в `GROWTH_PLAN.md`, которых раньше не было:**
   - `Texts/forum_bitcointalk.md` — образовательные посты без ссылки + подпись форума +
     порядок набора истории аккаунта (площадка банит за спам-ссылку с нового аккаунта).
   - `Texts/casino_directories.md` — список целевых каталогов + шаблоны листинга (короткий/
     развёрнутый).
   - `Texts/x_organic_content.md` — треды/твиты без ссылки, чтобы X-аккаунт не читался как
     spam-бот (сейчас 100% контента в `Generated/twitter_var_*` — чистый CTA).
   - `Texts/discord_server_structure.md` — структура собственного Discord-сервера + спека
     бота (не реализован, только спецификация).

## Приоритет по влиянию на доход (что дальше)

| # | Действие | Влияние на доход | Блокер |
|---|---|---|---|
| 1 | Разместить листинг на 3-5 casino-каталогах (`Texts/casino_directories.md`) | Высокое — устоявшийся канал именно для этой ниши, трафик уже "тёплый" (люди ищут казино-обзоры) | Нет — можно подавать формы каталогов уже сейчас |
| 2 | Завести аккаунт bitcointalk, начать 15-20 постов без ссылки | Среднее, но накопительное — открывает подпись-канал на несколько месяцев вперёд | Нет, но требует 2-3 недели на набор истории — начать сейчас, эффект позже |
| 3 | Собственный Discord-сервер (структура готова) | Среднее — актив, который не теряется при смене оффера | Нет для структуры; бот — отдельное решение по объёму |
| 4 | Google Search Console verification | Низкое прямое, но нужно для видимости SEO-эффекта остальных действий | Код от вас (вставлю за 1 минуту) |
| 5 | Apex-домен HTTPS (Namecheap/Cloudflare) | Низкое-среднее — сейчас нет прямых потерь трафика (redirect работает), но HTTPS-таймаут на apex — риск для части браузеров/ботов | API-токен или перенос DNS-зоны от вас |
| 6 | Блог-раздел для SEO | Высокое, но отложенное (недели на статьи + индексацию) | Ваше решение по объёму/срокам — это новый раздел сайта |
| 7 | Партнёрская панель BC.Game — проверить LTV/retention по subId | Критично для приоритизации всего остального (без этого не видно, какой канал реально даёт депозиты, а не просто клики) | Ваш логин в партнёрский кабинет |

## Рекомендация по порядку

Начать с **#1 и #2 параллельно** — оба не требуют доступа от вас, оба бьют по каналам с
подтверждённой конверсией именно в гемблинг-нише (в отличие от Reddit/LinkedIn/Product
Hunt/HN из исходного брифа, которые `GROWTH_PLAN.md` уже отклонил как нерелевантные или
рискованные для этой вертикали).

**#7 (доступ к панели BC.Game) — самый ценный запрос с моей стороны**: без реальных цифр
LTV/retention по subId весь план выше — гипотеза, приоритизированная по здравому смыслу, а
не по данным. Как только будет доступ — могу пересчитать приоритеты в этой таблице под
реальный ROI по каналам.

## Не буду делать (без изменений с предыдущего разговора)

Массовый скрапинг персональных данных людей на соцплощадках + холодный контакт от лица
казино-рефералки — юридический и репутационный риск (спам-жалобы, ToS-баны, реклама
гемблинга без учёта юрисдикции получателя), прямо противоречит GEMINI.md п.8. Каналы выше —
органический контент на площадках, где сама аудитория уже ищет то, что вы предлагаете.
