# CHANGELOG — ZHELEZO (zhelezo.space)

Реальные изменения по датам, в обратном хронологическом порядке. Не журнал сессий (см.
`Память/sessions/*.md` для полного контекста) — только то, что фактически изменилось в
продукте/коде. Заведён 2026-07-21 по запросу пользователя, когда main-поток принял на себя
постоянную роль CTO/Growth Engineer для проекта.

## 2026-07-21

- Создан этот файл и передана роль постоянного технического владельца проекта от фонового
  форка к основному потоку (по явному запросу пользователя) — во избежание повторного
  конфликта одновременного редактирования одних и тех же файлов (см. инцидент с
  `SalesAgent/bin/send.mjs` тем же днём ранее).
- **Fix**: добавлена skip-to-content ссылка (RU/EN/VI) — раньше клавиатурный/скринридер-
  пользователь был вынужден таб-ать через весь header на каждой странице.
- **Fix**: LanguageSwitcher теперь закрывается по Escape с возвратом фокуса на кнопку.
- **Chore**: `Scripts/brand-monitor/run-checks.bat` — обёртка для периодического запуска
  site-health-check.mjs + check-mentions.mjs, протестирована end-to-end.
- **Automation**: зарегистрирована задача Windows Task Scheduler `ZHELEZO-BrandMonitor` —
  ежедневный запуск `run-checks.bat` в 09:00, лог в `logs/monitor.log`. Подтверждено
  явным разрешением пользователя (системное изменение вне репозитория), проверено
  `schtasks /query` — статус Ready.
- **Fix (a11y)**: все 17 внешних ссылок (`target="_blank"`) на сайте теперь объявляют
  скринридеру "открывается в новом окне" (visually-hidden span, переведён на RU/EN/VI).
  Декоративные иконки внутри этих ссылок получили `aria-hidden="true"`, чтобы не дублировать
  объявление. Соотношение 17 ссылок / 17 меток проверено `grep` перед коммитом. Задеплоено,
  подтверждено `curl` против реального JS-бандла (`LocaleContext-*.js`) — по одному
  вхождению строки на каждый из 3 языков.
- **Fix**: `public/404.html` был русскоязычным независимо от того, в какой языковой секции
  (`/en/`, `/vi/`) пользователь словил битую ссылку — Cloudflare Pages отдаёт один и тот же
  статический 404-файл на все несовпавшие пути, отдельного 404 на директорию в статическом
  хостинге не бывает. Добавлен небольшой inline-скрипт без зависимостей, который определяет
  языковой префикс по URL и подменяет заголовок/текст/ссылку "на главную" на EN/VI; RU
  (без префикса) не тронут. Задеплоено, HTTP-код 404 сохранён, наличие скрипта подтверждено
  `curl` для `/en/` и `/vi/`.
- **Audited, ничего не меняли (честно, не для галочки)**: изображения (все статичные ассеты
  ≤56 КБ, оптимизировать нечего), шрифты (уже preconnect+preload+`display=swap`+noscript-
  фоллбэк — best practice), состав JS-бандла (крупнейший чанк 266 КБ / 86 КБ gzip — разумно
  для React-приложения, ничего раздутого), canonical-теги (все 24 HTML-страницы блога +
  3 языковые версии главной корректно self-referencing, проверено построчно), og:image:alt
  ("ZHELEZO Crypto Gaming" одинаков на всех языках — это имя бренда, не забытый перевод),
  формы на сайте отсутствуют (нечего валидировать), SalesAgent (`npm audit` чисто, синтаксис
  чист, TODO/FIXME не найдено — тех.долга не накопилось).
- **Security fix**: `SalesAgent/src/reports/index.mjs`'s `generateStats()` строил SQL через
  сырую интерполяцию строки (`WHERE created_at >= '${since}'`) вместо параметризованных
  запросов, как во всём остальном коде. Не эксплуатировался через текущие вызовы (оба
  передают только внутренние/белый-список значения), но был реальной дырой для любого
  будущего вызова с пользовательским `since`. Переписано на параметризованные `?`-запросы;
  проверено на реальном payload'е `OR '1'='1'` — фильтр больше не обходится. Коммит `586e2d0`.
- **Audited, ничего не меняли**: заголовки безопасности (`_headers` — CSP на точных хэшах,
  HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy — уже полный набор), git-история
  на секреты (полный `git log --all -p` по api-key/secret/password/token — только плейсхолдер
  в `.env.example`, `.env` никогда не коммитился), CRO дальше Hero.tsx (9+ CTA-точек по
  странице, `Header` уже `fixed top-0` — мобильный доступ к CTA уже решён), мёртвый код
  Landing (`noUnusedLocals`/`noUnusedParameters` в tsconfig + Tailwind JIT структурно не
  дают ему накапливаться).
- **Docs**: подтверждено пользователем напрямую — Google Search Console подтверждена, sitemap
  отправлен и доступен по `https://www.zhelezo.space/sitemap.xml`. Обновлён `ROADMAP.md` —
  этот пункт больше не блокер роста; HTML-тег verification в `index.html` так и остался
  плейсхолдером в комментарии (верификация прошла другим методом) — это не баг, не трогать.
- **Doc fix**: `SalesAgent/docs/ARCHITECTURE.md` утверждал "нигде в коде нет вызова email/SMTP
  API" — устарело с появлением `bin/send.mjs`. Обновлено под реальное текущее поведение.
- **Full redesign (по явному брифу пользователя)**: чёрный/тёмно-зелёный/неоновый зелёный
  (`#050505`/`#00FF66`) вместо прежней палитры cyberpunk-темы; новый canvas-фон с дрейфующими
  частицами пепла/искр + два медленных glow-пятна (не WebGL-библиотека — лёгкий 2D canvas,
  капped частиц, dpr≤2, пауза через Page Visibility API, статичный кадр при
  prefers-reduced-motion); glassmorphism на карточках Games/HowItWorks/FAQ/Hero-статистике/
  бонус-секции; scroll-reveal анимации (fade/blur/slide через framer-motion `whileInView`);
  Header — динамическая прозрачность/blur при скролле (была статичной); новые компоненты
  `SectionDivider` (глоу-разделители между секциями), `BackToTop`, `FollowMe` (8 соцсетей-
  заглушек — Instagram/Telegram/Telegram Channel/Facebook/X/TikTok/YouTube/Discord, заменил
  старый 3-иконочный блок в Footer); pure-CSS preloader в каждом index.html (RU/EN/VI, без
  цены в JS-бандле), снимается первым `useEffect` в App.tsx.
  **Проверено после деплоя, не на веру**: свежий Lighthouse на проде — Performance 82
  (не изменился, LCP даже улучшился 3.5с → 3.2с), Accessibility 100, SEO 100, Best Practices 77
  (не изменился, тот же добеплойный Яндекс.Метрика-кукис-кейс), CLS 0. Заголовки безопасности,
  все 33 URL из sitemap, canonical/hreflang/schema — без изменений/регрессий.
- **Premium-эскалация того же редизайна (тот же день, по повторному, более амбициозному брифу
  пользователя)**: magnetic-hover на 3 главных CTA (Hero/Header/бонус-секция, не на всех ссылках
  подряд — сознательно, ради иерархии), viewport-wide mouse-tilt на новый `EnergyCore` —
  "3D"-объект в Hero, собранный из слоёв CSS (conic-gradient кольца + radial-gradient пульсирующее
  ядро + орбитальные частицы, та же техника, что уже использовалась в LuckyWheel) вместо
  Three.js/WebGL — библиотека 3D была бы реальным риском для веса бандла и GPU, скрыт на экранах
  `<sm`, чтобы не перегружать мобильный hero. Новый `AnimatedCounter` — досчитывает число в
  статистике Hero при попадании в вьюпорт, сохраняя окружающий текст ("До 5 BTC"). Новые CSS-
  примитивы: `.shine-sweep` (диагональный блик по hover), `.gradient-border` (вращающаяся
  conic-gradient рамка — применена только к бонус-секции, не ко всем карточкам подряд, чтобы не
  плодить постоянно анимирующиеся элементы), `.noise-overlay` (inline SVG film-grain, opacity
  0.035, без запроса картинки).
  **Проверено после деплоя**: Lighthouse Performance остался **82** несмотря на добавленные
  постоянно анимирующиеся кольца/частицы/tilt/magnet/noise (LCP 3.3с, CLS 0, Accessibility/SEO
  100/100) — главный риск прохода не реализовался. Мобильный overflow проверен по коду
  (EnergyCore скрыт ниже `sm`, внешняя секция держит `overflow-hidden`, радиусы орбит сверены с
  размером контейнера), не скриншотами устройств — инструмента для этого в этой среде нет,
  честно отмечено как ограничение, а не выдано за полную проверку.
- **AAA-gaming визуальный язык (тот же день, третий заход по брифу пользователя — "не копируй
  существующий дизайн, создай новый")**: полированный металл/титан/карбон вместо плоского неона,
  chamfered ("срезанные") HUD-рамки вместо простых скруглённых прямоугольников
  (`.chamfered`/`.chamfered-sm`, clip-path), светящиеся угловые скобки (`CornerBrackets.tsx`,
  живут как соседи chamfered-панели, а не потомки — clip-path родителя обрезал бы их вместе с
  содержимым), металлический текст для H1 (`.metal-text`, хромовый градиент через
  background-clip:text). Новая панель по умолчанию — `.hud-panel` (стекло + металлическая
  рамка), заменила `.glass-card` на большинстве карточек (Hero-статистика, Games, HowItWorks CTA,
  FAQ, FollowMe). `.btn-metal` — объёмная "клавиша" с внутренним бликом/тенью вместо плоской
  заливки.
  **Реальная просадка Lighthouse, поймана и исправлена, не выдана за приемлемую**: первый деплой
  этого прохода — Performance 82 → **78** (LCP 3.3с → 3.7с). Причина: `.metal-text` изначально
  анимировал `background-position` (бесконечно) + двойной `drop-shadow` прямо на H1 — вероятном
  LCP-элементе Hero. Постоянно меняющийся фон/фильтр на LCP-кандидате не даёт браузеру засчитать
  его "стабильно отрисованным" вовремя. Исправлено — статичный градиент, один `drop-shadow`;
  передеплоено, перепроверено: Performance снова **82**, LCP снова **3.3с**. Урок закреплён
  комментарием в коде: анимированные/фильтрованные эффекты не класть на вероятные LCP-кандидаты.
- **По ходу того же прохода — 2 отдельных запроса пользователя**: (1) блок соцсетей перенесён из
  подвала в собственную секцию верхнего уровня (`Social.tsx`, `id="social"`) вместо низа страницы;
  (2) новый пункт навигации "Начать играть" (десктоп + мобайл) — прямая ссылка на реферальную
  ссылку в новой вкладке, а не скролл к секции. Оба — новые переведённые (RU/EN/VI) ключи
  `navSocial`/`navPlayNow` в `Header.tsx`.
