# Сессия 2026-07-19 (002) — SalesAgent: overnight-раунды 4-7 лидогенерации

## Контекст
Продолжение автономной ночной работы по всем проектам C:\Projects. Для CasinoReferral — самый
безопасный автономный кусок backlog: продолжить SalesAgent-лидогенерацию тем же методом, что и
сессии 010-011 (GPWA-каталог, новый keyword/страница на раунд, без пересечений с прошлыми
сидами). Ничего не отправлялось — только скоринг + черновики, как и раньше.

## Раунды
- **Раунд 4** (`seeds/2026-07-19-gpwa-round4-bitcoin-keyword.json`) — GPWA, keyword "bitcoin".
  8 сидов, 7 обработано (1 честно упал на fetch), 1 warm: btcgambling.com (score 58, черновик #9).
- **Раунд 5** (`.../round5-poker-keyword.json`) — GPWA, keyword "poker". 8 сидов, 6 обработано
  (2 честно упали), 2 warm: worldpokerdeals.com (54, черновик #10), yourpokerdream.com
  (64, черновик #11).
- **Раунд 6** (`.../round6-sportsbook-keyword.json`) — GPWA, keyword "sportsbook". 8 сидов,
  5 обработано (3 честно упали), 3 warm: opensportsbookusa.com (45, #12),
  offshoresportsbookfact.net (54, #13), ussportsbookreviews.com (50, #14).
- **Раунд 7** (`.../round7-roulette-keyword.json`) — GPWA, keyword "roulette". 8 сидов,
  8 обработано (0 упало), 2 warm: roulette-online.co.uk (56, #15), rouletteregeln.com (51, #16).

Итого за 4 раунда: 32 сида → 26 обработано, 6 честных fetch/http-ошибок (не выдумано),
8 новых warm-лидов, 8 новых черновиков первых писем (#9-#16). Хит-рейт по warm держится
14-60% по раундам, тренда на спад не увидел — остановился не потому что раунды перестали
быть продуктивными, а чтобы не штамповать лиды бесконечно без разбора (тот же принцип, что и
в прошлых сессиях: качество важнее количества).

## Баги
Не найдено — пайплайн отработал чисто на всех 4 раундах (супersede-логика черновиков и
фикс fallback-инсайта из сессий 010-011 держатся, дублирования текста не наблюдалось).

## Состояние CRM (после сессии)
36 компаний в базе (было 9 после сессии 011). 12 лидов в приоритете warm/hot суммарно
(включая старые). 16 черновиков всего (#1-16), часть — `rejected` (superseded), часть — `draft`,
готовы к ручной проверке и отправке.

## Дополнение (в ту же сессию): пользователь предоставил реальные данные отправителя
Пользователь прислал имя (Danil) и контакт (1ratking7@gmail.com) напрямую в чате. Обновил
`src/messages/config.mjs` (было "ВАШЕ ИМЯ" / "ВАША_ССЫЛКА_ИЛИ_EMAIL"). Так как черновики
рендерятся и сохраняются в БД в момент генерации (не читают конфиг заново при отправке),
перегенерировал все 13 активных (`status='draft'`) черновиков `first_email` через
`generateDraft()` — существующий supersede-механизм (сессия 011) автоматически перевёл старые
версии в `rejected` и создал новые (#17-29) с реальными именем/контактом. Проверено на лиде
#1 (tentenseven.com, draft #29) — текст содержит "Danil" / "1ratking7@gmail.com", плейсхолдеров
не осталось.

**Черновики теперь технически готовы к ручной проверке и отправке** — это был последний
код-блокер, дальше только ручная проверка текста и отправка пользователем.

## Дополнение 2 (в ту же сессию): построена автоматическая SMTP-отправка, пользователь
подтвердил и предоставил реальные креды — письма реально отправлены

Пользователь явно подтвердил ("Да, отправить все 13 автоматически") после того, как я
переспросил с прямым описанием риска (гемблинг-ниша, 13 получателей, без ручной проверки
каждого). Собрано:
- `src/messages/mailer.mjs` — тонкая обёртка над `nodemailer`, кидает исключение при ошибке,
  сама ничего не помечает отправленным.
- `bin/send.mjs` (`npm run send`) — по умолчанию dry-run, реальная отправка только с флагом
  `--confirm`, `--limit`/`--delay-ms` (по умолчанию 8с между письмами), лиды без найденного
  email пропускаются (только tentenseven.com — у него лишь контактная форма).
- `markDraftSent()` в `crm/index.mjs` теперь принимает опциональный `note` — чтобы отличать
  автоматическую отправку от ручной пометки; обновлён устаревший doc-комментарий (раньше
  утверждал, что в коде нет вызовов SMTP — это больше не так по архитектуре).
- `.env.example` — креды через `SMTP_*`, используется нативный `--env-file-if-exists`
  (Node 22+, без лишней зависимости `dotenv`). `.env` подтверждён как gitignored
  (`git check-ignore`) — реальный пароль в git никогда не попадал.

Пользователь прислал Gmail App Password напрямую в чате ("вписывай сам ..."). Записал в
`.env`, сначала проверил `verifySmtpConfig()` (SMTP-аутентификация прошла) — только потом
запустил реальную отправку, чтобы не спалить попытки на всех 13 сразу при плохом пароле.

**Реальная отправка**: `npm run send -- --all --confirm` — 12 из 12 писем отправлены успешно
(0 ошибок), лид #1 пропущен (нет email-контакта, только форма). Каждый успешный лид получил
запись в `message_drafts.status='sent'` + interaction-лог с пометкой "sent automatically via
SMTP (bin/send.mjs)".

**Это первая реальная холодная рассылка в рамках SalesAgent** — раньше только черновики.
Дальше — ждать ответов, обновлять `lead.status` вручную (`npm run lead -- <id> status
contacted`) по мере необходимости, следить за ответами на 1ratking7@gmail.com.

## Не сделано / дальше
- `lead.status` у всех 12 отправленных всё ещё `new` — `bin/send.mjs` намеренно не двигает
  статус лида (только черновик), чтобы не путать "письмо ушло" с "лид квалифицирован" —
  статус остаётся на усмотрение пользователя после первого ответа/его отсутствия.

## Дополнение 3: пользователь ушёл спать, попросил массовую рассылку ссылки на сам сайт
случайным людям ("пиши всем кому только можешь", "найди кому прислать мой сайт") — **отказано**.
Это принципиально другое действие, чем куративный B2B-питчинг SalesAgent: холодная реклама
гемблинг-сайта незнакомым людям без разбора — то же самое, что проект explicitно отклонял с
сессии 003 (GEMINI.md п.8: юр. риск рекламы гемблинга без учёта юрисдикции получателя,
спам-жалобы, риск блокировки только что использованного Gmail-аккаунта). Объяснил пользователю
почему, предложил легитимную альтернативу (X/GPWA/bitcointalk/Quora — уже одобренные каналы,
плюс платная реклама Meta/TikTok Ads как реальный быстрый вариант, если решит утром).
Пользователь принял, попросил продолжать безопасную работу всю ночь без него.

## Дополнение 4: раунды 8-10 (blackjack/baccarat/slots) + реальный баг в analyzer

Продолжил ту же лидогенерацию с явным ограничением: **никаких новых `--confirm`-отправок и
`markDraftSent()`** без пользователя в сознании — новые черновики остаются `draft` до его
проверки утром.

- **Раунд 8** (blackjack) — 8 сидов, 5 обработано (3 честных fetch-ошибки), 1 warm:
  playblackjackonline.co.uk (61, черновик #30).
- **Раунд 9** (baccarat) — 8 сидов, изначально 5 обработано (3 упали), но один "успех"
  (`baccarat.guru`) оказался багом: WAF-страница отказа доступа ("Sorry, your request has
  been denied." + IP + таймстемп, без реального `<title>`) не была Cloudflare-интерстишлом
  (нет auto-reload скрипта), поэтому существующий детектор ботов (заточенный под awisee.com
  в сессии 010) её пропустил — IP+таймстемп сохранился как "имя компании". Исправлено
  добавлением общего сигнала: реальное имя сайта/og:site_name никогда не является голым IP-
  адресом — проверка добавлена в двух местах (`isBotChallengePage()` и сразу после вычисления
  `name`). Проверено на живом URL — теперь корректно `blocked_by_bot_protection`. Бракованная
  запись (company id 45, lead id 44) вручную вычищена из БД тем же способом, что id 1/2 в
  сессии 011. После фикса реально 4 валидных + 1 warm: beto-spillemaskiner.dk (64, #31) —
  репрофилированный домен baccaratpro.dk, теперь датский slots-сайт.
- **Раунд 10** (slots) — 8 сидов, 8 обработано (0 fetch-ошибок), 1 warm: onlineslotshub.com
  (51, черновик #32). Признаки снижения качества источника: `winning-slots.com` вернул
  нерелевантный китайский спам-контент (доменный сквоттинг), `thailandslots.com` редиректит на
  несвязанный `pgslotsites.com` — не баг, тот же честный паттерн "устаревший домен", что и
  Dominobet/BETO Slots раньше; скоринг корректно не пустил оба в очередь на отправку (cold).

Остановился после раунда 10 — 3 раунда подряд с падающим, но не нулевым yield плюс явные
признаки шумного источника — разумная точка для паузы до утра, не "выжимать досуха".

Также проверено: **ClaudeTelegramBot** — процесс жив (PID 4420, тот же после рестарта в 04:45),
логи чистые с момента рестарта, старые `код 1`/300-секундные ошибки — все до фикса. **Facebook
SMMAgent** — `git status` чист, полный `py_compile`-прогон чист, изменений не вносил (по-прежнему
блокирован на реальные Meta API креды).

## Итог CRM на конец ночи
51 лид всего (было 9 на начало ночи). Черновиков #1-32 (часть `rejected`/superseded, 12 `sent`
реально, остальные `draft` — ждут проверки пользователем).

## Коммиты
`fff4b75` — 4 новых seed-файла (JSON) + записи памяти. `90c101b` — `config.mjs` (реальные
имя/контакт отправителя). `5d720c3` — SMTP auto-send tooling (без кредов). Реальный `.env` с
паролем не коммитился (gitignored). БД/дашборд/отчёты остаются gitignored, как и раньше.
