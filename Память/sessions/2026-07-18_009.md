# Сессия 2026-07-18 (009) — автономный режим, продолжение: privacy-gap + CSP CI-guard

## Что сделано (продолжение автономного цикла из сессии 008, без новых указаний пользователя)

1. **Privacy Policy gap найден и задокументирован**: `Landing/public/privacy-policy.html`
   прямо утверждает "мы не собираем имена/email напрямую", но `Scripts/discord-bot/store.js`
   хранит Discord username + user ID в `data/spins.json`. Бот ещё не задеплоен (нет токена),
   поэтому это не активное нарушение — но добавлен явный пункт в
   `Scripts/discord-bot/README.md` ("⚠️ Перед первым реальным запуском — обновить Privacy
   Policy"), чтобы это не забылось в момент реального запуска.
2. **CSP-хэши: ручная дисциплина → автоматическая проверка в CI**. Весь проект последовательно
   полагался на то, что кто-то (я) не забудет прогнать `csp-hashes.mjs` после правки инлайн-
   скрипта — реальный риск человеческой памяти. Написан `Landing/scripts/verify-csp-hashes.mjs`
   — пересчитывает хэши из собранного `dist/index.html` и сверяет с `public/_headers`,
   падает с чётким сообщением при расхождении. **Проверено, что реально ловит расхождение**:
   намеренно испортил один хэш в `_headers`, скрипт корректно упал с описанием проблемы;
   восстановил файл, перепроверил — чисто. Подключено в `.github/workflows/build.yml` как шаг
   после `npm run build`.
3. **Обновлены `Experts/Security/SYSTEM.md` и `Experts/DevOps/SYSTEM.md`** с упоминанием нового
   CI-guard — живая демонстрация правила "постоянное улучшение" из `CLAUDE.md` (новая практика
   → сразу в соответствующий SYSTEM.md, не только в `Память/`).

## Итог автономного цикла (сессии 008-009)
Устранены 2 реальных расхождения документации с фактическим состоянием кода (README×2,
HYPOTHESES.md), задокументирован 1 будущий compliance-гэп (privacy policy vs Discord-бот),
добавлена 1 новая инициатива с обоснованием (EN-пилот блога), автоматизирована 1 ручная
проверка (CSP-хэши → CI). Все изменения собраны в реальные факты этого проекта, не выдуманы
для видимости прогресса.

4. **FAQ.tsx**: добавлен вопрос "Нужен ли отдельный промокод для бонуса?" на главную страницу
   — тот же вопрос, на который уже отвечает статья блога, но посетитель, не долиставший до
   блога, раньше не получал ответа на лендинге вообще. Усиливает и FAQPage schema.org (массив
   `faqs` маппится прямо в JSON-LD).

## Коммиты
Всё закоммичено, запушено, задеплоено в рамках этого автономного цикла (сессии 008-009):
`997d9cb`, `a76983d`, `9bff407`. Проверено на живом сайте после каждого деплоя (включая
проверку CI на самом GitHub, не только локально).
