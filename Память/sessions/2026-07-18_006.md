# Сессия 2026-07-18 (006) — блог: 3 оставшиеся статьи + Discord-бот (auto mode)

## Контекст
Пользователь: "делай всё дальше по плану" под Auto Mode — продолжил без дополнительных
уточнений по `REPORT/ACTION_PLAN.md`, выбрав то, что реально выполнимо без интерактивного
доступа к внешним аккаунтам (казино-каталоги/bitcointalk регистрация — по-прежнему не могу
сам; блог и код бота — могу).

## Что сделано

1. **3 оставшиеся статьи блога** (все 5 тем из `GROWTH_PLAN.md` теперь закрыты):
   - `blog/bc-game-otzyvy/` — обзор с явным раскрытием партнёрства ("это не независимый обзор")
     в начале статьи, честные оговорки (лицензия Curaçao не эквивалент MGA/UKGC, Provably Fair
     не покрывает слоты сторонних провайдеров).
   - `blog/crash-plinko-strategiya/` — математика игр, явно НЕ обещает выигрышных стратегий:
     Мартингейл описан как "не меняет ожидаемое значение, увеличивает риск разорения", а не как
     рабочий метод.
   - `blog/no-kyc-crypto-casino/` — что значит "без KYC" технически, где реальный компромисс
     (формальные рычаги при спорной транзакции), а где нет (легальность жилья/честность игр —
     отдельные вопросы, не связанные с KYC-политикой).
   - Полная перелинковка между всеми 5 статьями + существующими двумя, `subId2=<slug>` на
     каждой реф-ссылке.
2. **Discord-бот реализован** (`Scripts/discord-bot/`) — `!spin`/`!bonus`/`!faq`/`!leaderboard`
   по спеке `Texts/discord_server_structure.md`. discord.js v14, JSON-файл для лидерборда
   (`data/spins.json`, в `.gitignore`). Проверено: синтаксис, `npm install` резолвится,
   `node bot.js` без токена падает с понятным сообщением (ожидаемое поведение — токена у меня
   нет и быть не может), логика `store.js` (spin/leaderboard) протестирована напрямую и работает
   корректно. **Не задеплоено** — нет ни токена бота, ни хостинга для постоянного процесса,
   оба пункта нужны от вас (`Scripts/discord-bot/README.md` — пошаговая инструкция).
3. `vite.config.ts`/`blog/index.html`/`sitemap.xml`/`blog/README.md` обновлены под 5 статей.
4. Билд проверен, CSP-хэши пересчитаны и совпадают с уже прописанными в `_headers` (инлайн-
   скрипты в `index.html` не менялись).

## Не сделано (по-прежнему требует интерактивного доступа)
Регистрация в casino-каталогах, bitcointalk-аккаунт, запуск Discord-бота на реальном сервере —
всё требует логина/создания аккаунта на стороне пользователя, не изменилось с прошлых сессий.

## Коммиты
Не коммичено на момент записи.
