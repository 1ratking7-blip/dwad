# Context — постоянная информация о пользователе и проекте

*Только устойчивые факты, не временное состояние — временное в summary.md/todo.md*

## Пользователь
- Email: 1ratking7@gmail.com
- Ведёт партнёрский (affiliate) маркетинг-проект под BC.Game, бренд ZHELEZO
- Аккаунт Cloudflare привязан к тому же email

## Роль ассистента в этом проекте (из GEMINI.md)
Ведущий эксперт по affiliate-маркетингу, SEO, SMM, UX/UI, веб-аналитике, CRO — см. `GEMINI.md` в корне проекта для полного регламента.
Ключевой принцип из GEMINI.md: юридическая чистота — избегать агрессивного кликбейта/прямого обмана, использовать pre-lander'ы и нативный маркетинг вместо жёсткого спама.

## Стиль работы, который пользователь подтвердил
- Может явно попросить **не задавать вопросов** и работать полностью автономно, исправляя проблемы сразу по мере обнаружения (не просто предлагать) — так было в задаче полного аудита 2026-07-18.
- В обычном режиме (не "не задавай вопросов") — дефолтное поведение: не коммитить и не пушить без явного запроса.
- Любит короткие финальные отчёты по категориям/этапам с конкретными числами (было/стало), а не общими фразами.
- Просит `git diff` / проверку после каждого этапа, когда работа идёт по частям.
- Ценит, когда я нахожу и чиню баги не только по чек-листу, но и через реальную проверку (сборка, браузер, curl на проде) — не просто "написал код и сказал что работает".

## Технический стек проекта
- `Landing/` — React 18 + TypeScript + Vite + Tailwind, framer-motion, lucide-react
- Деплой: Cloudflare Pages через `wrangler` (`cd Landing && npm run deploy`), НЕ Netlify/Vercel
- Домен: www.zhelezo.space, DNS через Namecheap API (apex редиректит на www)
- Аналитика: GA4 (`G-0RLZ7LGT08`), Яндекс.Метрика (`110820422`), dataLayer — уже живые
- CSP: `Landing/public/_headers`, хэши пересчитываются `Landing/scripts/csp-hashes.mjs`

## Структура проекта (верхний уровень)
```
CasinoReferral/
├── Landing/       — сайт (React/Vite)
├── Scripts/       — генератор контента для соцсетей
├── Texts/         — контент-стратегия, посты
├── Banners/       — ТЗ на баннеры для дизайнера
├── Analytics/      — HYPOTHESES.md и A/B-гипотезы
├── Images/, Videos/, Assets/
├── GEMINI.md      — регламент роли агента
└── Память/        — эта папка, постоянная память между сессиями
```
